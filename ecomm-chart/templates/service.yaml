apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecomm-chart.fullname" . }}
  labels:
    {{- include "ecomm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecomm-chart.selectorLabels" . | nindent 4 }}
